#include "headers.h"


            /*if(ball.cent_x>all_tiles[i].cent_x){
                if(ball.cent_y>all_tiles[i].cent_y){
                    x1 = all_tiles[i].x + all_tiles[i].width;
                    y1 = all_tiles[i].y + all_tiles[i].height;
                }else{
                    if(ball.cent_y<all_tiles[i].cent_y){
                        x1 = all_tiles[i].x + all_tiles[i].width;
                        y1 = all_tiles[i].y;
                    }
                }
            }else{
                if(ball.cent_y>all_tiles[i].cent_y){
                    x1 = all_tiles[i].x;
                    y1 = all_tiles[i].y + all_tiles[i].height;
                }else{
                    if(ball.cent_y<all_tiles[i].cent_y){
                        x1 = all_tiles[i].x;
                        y1 = all_tiles[i].y;
                    }
                }
            }
            L = pow(ball.cent_x - x1,2) + pow(ball.cent_y - y1,2);
            dist = sqrt(L);
            if(dist < 8){
                ball.vel_x = -ball.vel_x;
                ball.vel_y = -ball.vel_y;
                all_tiles[i].visibility = 1;
            }*/
